import 'package:flutter/material.dart';

class RightPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
        child: new Center(
            child: new Icon(Icons.airline_seat_flat, size: 150.0, color: Colors.red)
        )
    );
  }
}